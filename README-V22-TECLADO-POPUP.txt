Dona Flor V22 - Teclado + Pop-up corrigido

Correções:
- O teclado do celular não fecha mais o pop-up de edição.
- Remove fechamento agressivo por clique fora que era disparado pelo teclado/resize.
- Pop-up continua fechando no X e após salvar.
- Ajusta altura do pop-up quando o teclado está aberto.
- Mantém relatórios com filtros, CSV, botão sair e correções anteriores.

SQL:
- Não precisa SQL.
